# Домашнє завдання до Теми Основи об’єктно-орієнтованого програмування

У цьому завданні ви моделюєте невелику систему, схожу на ті, з якими стикаються розробники в реальних проєктах. Завдяки цьому попрактикуєтесь у застосуванні ключових принципів ООП — інкапсуляції, композиції, наслідування і поліморфізму — у зв’язному, життєвому контексті.

## Опис завдання

Вам потрібно реалізувати модель бібліотеки з підтримкою об'єктів: книги, автори, фізичні примірники, читачі, бібліотека як агрегатор. Основна мета — реалізувати предметну область з використанням ключових понять ООП у TypeScript.

## Очікувана функціональність

### Основні класи:

- **`Author`** зберігає ім’я та список написаних книг.
- **`Book`** реалізує `AbstractBook`, містить назву, рік, автора.
- **`EBook`** реалізує `AbstractBook`, додає поле `url`.
- **`Copy`** містить посилання на книгу та прапорець `isAvailable`.
- **`Reader`** має унікальний `id`, ім’я та список позичених копій.
- **`Library`** надає методи для:
  - додавання книг, авторів, копій, читачів
  - отримання вільних копій
  - пошуку книг за автором
- **`BorrowService`** окремий сервіс для позичання книги читачу `borrow(reader: Reader, copy: Copy)`.


## Загальні технічні моменти для ДЗ 1

- `.gitignore` - це мастхев! Найчастіша помилка: завантаження папки `node_modules` у репозиторій. Вона важить багато і не потрібна в GitHub. Переконайтеся, що файл `.gitignore` створений і містить рядок `node_modules/` до першого комміту.
- Zip-архів + Посилання на GitHub. Не забудьте: для перевірки потрібно надіслати і посилання на репозиторій, і zip-архів (названий за форматом `ДЗ_Номер_ПІБ`).
- Команда запуску. Ваш код повинен запускатися командою `npx ts-node src/main.ts`. Перевірте це перед відправкою.


Тема 1. На що звернути увагу:

- Абстрактні класи: Клас AbstractBook має бути саме abstract. Спробуйте створити його екземпляр (new AbstractBook(...)) - якщо TypeScript видає помилку, ви все зробили правильно.
- Інкапсуляція: Не робіть усі поля public. Використовуйте private або protected для внутрішніх даних, а доступ надавайте через геттери та сеттери.
- Поліморфізм: Метод getDescription() у класі Book та EBook повинен повертати різний текст. Це і є суть поліморфізму - однаковий виклик, але різна поведінка.
- Спадкування: EBook має розширювати AbstractBook, а не копіювати його код.

## Критерії оцінювання (100 балів):  

Категорія | Балів
-------- | -----
Реалізація інкапсуляції у всіх класах: | 10
Коректне наслідування EBook -> AbstractBook: | 10
Реалізація поліморфізму через getDescription(): | 10
Правильне застосування композиції в Library: | 10
Реалізація сервісу BorrowService: | 10
Заборона створення AbstractBook: | 5
Наявність прикладу повної взаємодії у main.ts: | 15
Структура проекту відповідає вимогам: | 10
Якість іменування, форматування, розбивка коду: | 10
Без помилок типізації у TypeScript: | 10 

## Приклад виконання:
```sh   
Attempting to borrow copy1...
Borrow result: true
Attempting to borrow copy1 again...
Borrow result: false
Attempting to return copy1...
Copy1 is available: true

Book descriptions:
Physical book "The Great Book" by John Doe (2020)
E-book "Digital Book" by John Doe (2021) - Available at: <https://example.com/ebook>  
```

## Команда запуску:
```sh
npx ts-node hw01/src/main.ts
```